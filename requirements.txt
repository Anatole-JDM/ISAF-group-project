# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
requests>=2.31

# Models — the three arms required by the brief
scikit-learn>=1.3      # white-box: logistic regression scorecard
optbinning>=0.19       # WOE binning for the scorecard
xgboost>=2.0           # ML arm
tabpfn>=2.0            # tabular foundation model arm

# Interpretability
shap>=0.44

# Fairness
fairlearn>=0.10

# Viz + app
matplotlib>=3.7
seaborn>=0.13
streamlit>=1.30
